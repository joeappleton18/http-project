<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="main.css">
	<title>Hello There</title>
</head>

<body>
	<h1> Welcome to Joe's Gif Chat </h1>

	<h2> Headers </h2>
	<!-- 

		for (const x in headers) {
		console.log(`${x}: ${headers[x]}`);
	}
	 -->

	<section class="code">
		<% for (const x in headers) { %>

			<%= x %>: <%= headers[x] %><br>

					<% } %>

	</section>
</body>

</html>